import React from "react";

const AboutPage = () => {
  return (
    <div>
      <h2>About</h2>
      <p>
        This App uses React, React Hooks for api requests, React router for
        navigation and redux for state management and redux-thunks for handling
        side effects.This app fetches data from the mock api.
      </p>
    </div>
  );
};

export default AboutPage;
